---
title: Oracle
description: Configure Oracle source and target prerequisites for snapshots and change data capture
sidebar:
  order: 4
ai:
  kind: connector
  id: oracle
  category: databases
  maturity: ga
  useAs: [source, target]
  modes: [snapshot, cdc]
  aliases: [oracle snapshot, oracle cdc, oracle logminer, oracle source, oracle target]
---

import { Aside, ConnectorCapabilities, ConnectorProfile } from '@/components/mdx';
import { SourceModeTab, SourceModeTabs } from '@/components/source-mode-tabs';

Use Oracle with TapState for snapshots, CDC, DDL capture, target writes, and DDL apply.

<ConnectorProfile
  category="Databases"
  maturity="GA"
  maturityLabel="General availability"
  worksAs="Source, Target"
  capabilities="Snapshot, CDC, DDL capture, DDL apply"
  compatibility="Oracle 9i, 10g, 11g, 12c, 18c, 19c+ · Single instance and RAC"
/>

<ConnectorCapabilities
  source="Snapshot and CDC"
  target="Data write"
  schema="DDL capture and apply"
/>

<Aside type="note" title="Local validation boundary">
  The bundled offline catalog does not contain Oracle. `tapstate validate` accepts the resource structure, but defers Oracle-specific fields and modes to the server-side connector contract. This does not indicate that Oracle is unsupported by a TapState deployment.
</Aside>

## Before you begin

### Source

<SourceModeTabs label="Choose the Oracle source read mode">
<SourceModeTab value="snapshot">

Use **Snapshot** to copy the rows that exist now.

1. Create a dedicated Oracle account and grant session access.
2. Grant dictionary and object-read permissions required for the selected schemas and tables.
3. In multitenant environments, create the account and grants in the correct CDB or PDB context.

Snapshot does not require ARCHIVELOG, supplemental logging, or LogMiner.

</SourceModeTab>
<SourceModeTab value="snapshot-cdc">

Use **Snapshot + CDC** to copy existing rows and then capture redo-log changes.

1. Grant the Snapshot permissions plus the LogMiner and transaction-log access required by your selected CDC method.
2. Enable `ARCHIVELOG` and database-level supplemental logging.
3. Enable supplemental logging for participating tables and verify that archived logs cover the recovery window.
4. Grant `LOGMINING` on Oracle 12c and later when using LogMiner.
5. Prepare DDL-capture permissions only when DDL collection is required.

Oracle CDC methods have different architecture and operational requirements. Confirm the method configured by your TapState deployment before applying method-specific settings.

</SourceModeTab>
</SourceModeTabs>

### Target

Prepare a dedicated writer with session, object creation, and DML privileges for the destination schema. Add `ALTER` and other DDL privileges only when DDL apply is explicitly allowed.

## Create a connection

Create the Oracle connection through the connection flow supplied by your TapState deployment. The public offline catalog does not contain an Oracle field schema, so this page does not publish a speculative `.cyn.yml` example.

## Validate the configuration

The bundled offline CLI cannot validate Oracle field names, modes, credentials, logging, supplemental logging, or LogMiner access. Use your deployment's connection validation and a staging run to confirm them.

## Limitations

- LogMiner and raw-log capture have different type, architecture, and operational constraints.
- Oracle empty strings become `NULL`; heterogeneous targets and constraints require staging tests.
- Connection encryption settings depend on the Oracle connector contract supplied by your deployment; this page does not assume a field name or TLS behavior.

## Reference

| Signal | Supported baseline |
|---|---|
| Source modes | Snapshot and CDC |
| Target | Data write and DDL apply |
| DDL | Capture and apply |
| Versions | Oracle 9i, 10g, 11g, 12c, 18c, 19c+ |
