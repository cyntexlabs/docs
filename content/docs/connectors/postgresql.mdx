---
title: PostgreSQL
description: Configure PostgreSQL source and target prerequisites for snapshots and change data capture
sidebar:
  order: 3
ai:
  kind: connector
  id: postgres
  category: databases
  maturity: ga
  useAs: [source, target]
  modes: [snapshot, cdc]
  aliases: [postgresql snapshot, postgresql cdc, postgres wal, postgresql source, postgresql target]
---

import { Aside, ConnectorCapabilities, ConnectorProfile } from '@/components/mdx';
import { SourceModeTab, SourceModeTabs } from '@/components/source-mode-tabs';

Use PostgreSQL with TapState for snapshots, CDC, DDL capture, target writes, and DDL apply.

<ConnectorProfile
  category="Databases"
  maturity="GA"
  maturityLabel="General availability"
  worksAs="Source, Target"
  capabilities="Snapshot, CDC, DDL capture, DDL apply"
  compatibility="PostgreSQL 9.4–16"
/>

<ConnectorCapabilities
  source="Snapshot and CDC"
  target="Data write"
  schema="DDL capture and apply"
/>

<Aside type="note" title="Local validation boundary">
  The bundled `postgres` catalog has connection fields, but does not declare source modes or target write capability. Local validation can check field shape only; it cannot establish whether a deployment accepts a PostgreSQL mode or target configuration.
</Aside>

## Before you begin

### Source

<SourceModeTabs label="Choose the PostgreSQL source read mode">
<SourceModeTab value="snapshot">

Use **Snapshot** to copy the rows that exist now.

1. Create a dedicated login with `CONNECT` access to the database.
2. Grant `USAGE` on selected schemas and `SELECT` on selected tables.
3. Grant access to future tables when table discovery must include objects created later.

Snapshot does not require logical WAL, a replication slot, or a decoding plugin.

</SourceModeTab>
<SourceModeTab value="snapshot-cdc">

Use **Snapshot + CDC** to copy existing rows and then follow WAL changes.

1. Grant the Snapshot permissions plus replication access.
2. Set `wal_level = logical` and size `max_replication_slots` and `max_wal_senders` for the planned tasks.
3. Choose the supported decoding path—`pgoutput`, `wal2json`, or `decoderbufs`—and install it when required.
4. Set `REPLICA IDENTITY` appropriately for tables whose updates or deletes must include enough key data.
5. For DDL capture, prepare the event-trigger and audit-table permissions described by the reused connector.

</SourceModeTab>
</SourceModeTabs>

### Target

Prepare a dedicated writer with schema usage, table creation when TapState may create targets, and the required `INSERT`, `UPDATE`, and `DELETE` privileges. Add DDL privileges only when DDL apply is allowed.

## Create a connection

Create the PostgreSQL connection through the connection flow supplied by your TapState deployment. The public offline catalog is incomplete, so this page avoids publishing a mode- or target-specific `.tapstate.yml` example that could be misleading.

## Validate the configuration

The bundled offline CLI can validate known PostgreSQL field shapes, but cannot validate source modes, target capability, credentials, replication settings, or a live database connection. Confirm those with your deployment's connection validation and a staging run.

## Limitations

- Replication slots retain WAL; monitor disk growth and remove abandoned slots through an approved operational process.
- Tables without a suitable replica identity can produce incomplete update or delete keys.
- Connection encryption settings depend on the PostgreSQL connector contract supplied by your deployment; this page does not assume a field name or TLS behavior.

## Reference

| Signal | Supported baseline |
|---|---|
| Source modes | Snapshot and CDC |
| Target | Data write and DDL apply |
| DDL | Capture and apply |
| Versions | PostgreSQL 9.4–16 |
