---
title: Vika
description: Configure Vika as a TapState data-writing target connection
sidebar:
  order: 53
ai:
  kind: connector
  id: vika
  category: saas-business-commerce-apis
  maturity: preview
  useAs: [target]
  modes: []
  aliases: [vika, vika target, vika data write, vika api]
---

import { Aside, ConnectorProfile, ValidationStatusGuide } from '@/components/mdx';

Use Vika as a data-writing target. This connection does not provide a source read mode.

<ConnectorProfile
  category="SaaS, business & commerce APIs"
  maturity="Preview"
  maturityLabel="Available for evaluation"
  worksAs="Target"
  capabilities="Data write"
  compatibility="Vika API"
/>

<Aside type="note" title="Rate limits are service-side">
  Keep requests within the table and API limits of the selected Vika plan. Local validation does not authenticate, measure rates, or test a write.
</Aside>

## Before you begin

### Target

1. Choose the Vika space and target tables that the pipeline may write.
2. Create a credential with the minimum write scope and keep it outside source control.
3. Confirm the selected Vika plan's table, row, and API-rate limits with the account administrator.

TapState supports `upsert` and `append` write semantics, but it does not define field mapping, conflict selection, rate-limit handling, or schema evolution behavior.

## Create a connection

```yaml title="tapstate-work/source/operations_vika.tapstate.yml"
version: tapstate/v1
kind: source
id: operations_vika
connector: vika
config:
  hostUrl: https://api.vika.cn
  spaceId: ${VIKA_SPACE_ID}
  credential: ${VIKA_CREDENTIAL}
```

Target connections have no source `mode` or `tables` section. `hostUrl` defaults to `https://api.vika.cn`.

## Validate the configuration

```bash
tapstate validate --workdir tapstate-work
```

<ValidationStatusGuide />

Local validation checks the resource envelope and recognized fields. It does not authenticate to Vika, list spaces or tables, enforce a rate limit, map fields, or write a record.

## Limitations

- TapState supports target capability only. It does not declare source reads, Snapshot, CDC, API mode, or DDL handling.
- Verify Vika API limits, table permissions, schema mapping, and conflict behavior against the deployed runtime before production use.
- Keep `credential` in an environment variable or a secret manager.

## Reference

| Field | Required | Default | Use |
|---|---|---|---|
| `hostUrl` | Yes | `https://api.vika.cn` | Vika API base URL. |
| `spaceId` | Yes | — | Vika space identifier. |
| `credential` | Yes | — | Vika credential; inject through an environment variable. |
