---
title: Lark Bitable
description: Configure Lark Bitable (Feishu Bitable) as a TapState API source
sidebar:
  order: 40
ai:
  kind: connector
  id: feishu-bitable
  category: saas-business-commerce-apis
  maturity: preview
  useAs: [source]
  modes: [api]
  aliases: [lark bitable, feishu bitable, feishu multi-dimensional table, lark base, bitable api]
---

import { ConnectorProfile, ValidationStatusGuide } from '@/components/mdx';

Use Lark Bitable, also known as Feishu Bitable, to read records from one Bitable table through the platform API.

<ConnectorProfile
  category="SaaS, business & commerce APIs"
  maturity="Preview"
  maturityLabel="Available for evaluation"
  worksAs="Source"
  capabilities="API read, Resource discovery"
  compatibility="Lark / Feishu Bitable API"
/>

## Before you begin

### Source

1. Create or select a Lark/Feishu self-built application.
2. Record its App ID and App Secret.
3. Grant the application access to the Bitable application and table that TapState will read.
4. Copy the Bitable App Token and Table ID from the Bitable URL or API tooling.

![Locate the App ID and App Secret in Lark or Feishu Open Platform](/images/connectors/feishu-app-credentials.png)

The Bitable API enforces rate limits. Design polling and retries for the limits published by Lark/Feishu, and verify the selected application scopes with a representative table.

## Create a connection

```bash
tapstate new --non-interactive \
  --kind source \
  --connector feishu-bitable \
  --id customer_requests \
  --mode api \
  --set app_id='${LARK_APP_ID}' \
  --set app_secret='${LARK_APP_SECRET}' \
  --set app_token='${LARK_BITABLE_APP_TOKEN}' \
  --set table_id='${LARK_BITABLE_TABLE_ID}' \
  --workdir tapstate-work
```

The resource uses these exact field spellings:

```yaml title="tapstate-work/source/customer_requests.tapstate.yml"
version: tapstate/v1
kind: source
id: customer_requests
connector: feishu-bitable
config:
  app_id: ${LARK_APP_ID}
  app_secret: ${LARK_APP_SECRET}
  app_token: ${LARK_BITABLE_APP_TOKEN}
  table_id: ${LARK_BITABLE_TABLE_ID}
mode: api
```

All four fields are required. Always keep `app_secret` in an environment variable and out of source control.

## Validate the configuration

```bash
tapstate validate --workdir tapstate-work
```

<ValidationStatusGuide />

## Limitations

- This connector reads Bitable data only. It does not write records or apply schema changes.
- App permissions, tenant policy, and API rate limits are enforced by Lark/Feishu at runtime.
- Do not put App Secret values in a committed `.tapstate.yml` file.

## Reference

### Supported operations

| Direction | Data operations |
|---|---|
| Source API | Read records from the selected Bitable table |
| Target DML (`INSERT`, `UPDATE`, `DELETE`) | Not available |

### Connection settings

| Field | Required | Use |
|---|---|---|
| `app_id` | Yes | Self-built application App ID. |
| `app_secret` | Yes | Self-built application secret. Supply through an environment variable. |
| `app_token` | Yes | Bitable application token. |
| `table_id` | Yes | Bitable table identifier. |
